# -numpy-mlp
这是一个纯由 numpy 搭建，未使用 pytorch 的简单 mlp。
内部框架较为完善，类内部与类之间的逻辑清晰，专门抽象了一层 optimizer 负责参数更新。
所有超参数和激活函数都使用字典存储（激活函数只写了 ReLU 函数，可根据需要自行添加），类里也封装好了一些函数，如测试集专用的 evalute 函数。
目前本项目做到了 28 * 28 输入 26 输出的字母识别，准确率在 89% 左右。可根据需求自行改变 layerlist 适配不同输入输出或者调整隐藏层。
